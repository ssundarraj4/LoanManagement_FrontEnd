import { stringify } from "querystring";

export class User {
    userId: string;
    password: string;
    cpassword: string;
    firstName: string;
    lastName: string;
    email: string;
    contact: number;
    
}